# ==========================================
# MEDALLION ETL - Dependencias
# ==========================================

# === CONFIGURACIÓN ===
pydantic>=2.0
pydantic-settings>=2.0

# === BASE DE DATOS ===
sqlalchemy>=2.0
psycopg2-binary>=2.9

# === UTILIDADES ===
python-dateutil>=2.8
python-dotenv>=1.0

# === API CLIENT ===
# chesserp se instala por separado desde el repo local de cada entorno:
#   Dev:    pip install -e /home/nahuel/projects/work/chesserp-py-sdk
#   Server: pip install -e /srv/app/chesserp-api (via post-receive hook)

# === TESTING ===
pytest>=7.0
pytest-mock>=3.10
